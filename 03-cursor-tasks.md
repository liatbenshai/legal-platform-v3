# רשימת משימות ל-Cursor - בניית גרסה 3

מסמך זה נכתב כדי שתוכלי להעתיק כל משימה ל-Cursor כפרומפט עצמאי. כל משימה כוללת **הגדרת מוכן** ברורה כדי שתוכלי לוודא שהיא הושלמה לפני המעבר לבאה.

חשוב: לפני כל משימה, צרפי ל-Cursor את שני המסמכים הקודמים (ארכיטקטורה ומפרט מערכת המגדר) כקונטקסט.

---

## שלב 0: הקמת תשתית

### משימה 0.1 - הקמת פרויקט Next.js חדש

**מה לעשות:**
צור פרויקט Next.js 14 חדש בשם `legal-platform-v3` עם TypeScript, Tailwind, App Router. התקן את התלויות הבאות:
- `@supabase/supabase-js`
- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`
- `docx`, `file-saver`
- `lucide-react`
- `@anthropic-ai/sdk`
- `clsx`, `tailwind-merge`
- `zod` (לוולידציה)
- `vitest`, `@testing-library/react` (לבדיקות)

הגדר `tsconfig.json` עם paths: `@/*` → `./src/*`.

צור קובץ `.env.local.example` עם המשתנים הנדרשים: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `ANTHROPIC_API_KEY`.

הגדר תמיכת RTL בקובץ `layout.tsx` ובconfig של Tailwind.

**מוכן כאשר:**
- `npm run dev` מפעיל את הפרויקט בלי שגיאות
- דף הבית מציג טקסט "Legal Platform v3" בעברית מימין לשמאל
- `npm run build` עובר בלי שגיאות TypeScript

---

### משימה 0.2 - הגדרת Supabase

**מה לעשות:**
צור פרויקט Supabase חדש (השאירי את הקיים בצד).

צור את הטבלאות הבאות לפי סכמת בסיס הנתונים במסמך הארכיטקטורה: `clients`, `persons`, `documents`, `document_versions`, `section_templates`, `user_section_templates`.

הוסף לכל טבלה Row Level Security policies שמבטיחים שמשתמש רואה רק את הנתונים שלו.

צור `supabase/migrations/001_initial_schema.sql` עם כל ה-DDL.

צור קובץ `src/lib/db/supabase.ts` שיוצר client.

**מוכן כאשר:**
- כל הטבלאות קיימות ב-Supabase
- ניתן להריץ `supabase db push` והכל מתבצע
- RLS פעיל על כל הטבלאות
- בדיקה ידנית: יצירת record דרך Supabase Studio עובדת

---

### משימה 0.3 - הגדרת אימות

**מה לעשות:**
הקם מערכת אימות בסיסית עם Supabase Auth:
- `/login` - דף התחברות
- `/register` - דף הרשמה
- `/logout` - פעולת התנתקות
- middleware שמגן על כל ה-routes חוץ מ-`/login` ו-`/register`

צור hook `useUser()` ב-`src/lib/hooks/useUser.ts`.

**מוכן כאשר:**
- אפשר להירשם, להתחבר, להתנתק
- ניסיון לגשת לדף מוגן בלי התחברות מפנה ל-`/login`
- ירון יכול ליצור חשבון ולהתחבר

---

## שלב 1: מודלים בסיסיים ומנוע

### משימה 1.1 - הגדרת מודלים

**מה לעשות:**
צור את כל קבצי ה-types ב-`src/lib/types/` לפי מסמך הארכיטקטורה:
- `person.ts`
- `client.ts`
- `document.ts`
- `template.ts`
- `index.ts` - מייצא הכל

צור גם schemas של zod לכל מודל ב-`src/lib/schemas/` לוולידציה.

**מוכן כאשר:**
- כל המודלים מוגדרים
- `npm run build` עובר
- ניתן לייבא טיפוס מכל מקום בקוד עם `import { Person } from '@/lib/types'`

---

### משימה 1.2 - מילון המגדר

**מה לעשות:**
צור את `src/lib/engine/dictionary.ts` והעתק לתוכו את המילון המלא ממסמך מפרט מערכת המגדר.

הוסף את הטיפוס `InflectedWord` ופונקציית עזר `hasWord(word: string): boolean`.

**מוכן כאשר:**
- הקובץ קיים עם 90+ ערכים
- TypeScript מזהה את הטיפוסים
- `hasWord('מצהיר')` מחזיר `true`
- `hasWord('שטויות')` מחזיר `false`

---

### משימה 1.3 - מנוע ה-Rendering

**מה לעשות:**
צור את `src/lib/engine/renderer.ts` עם המימוש המלא לפי מסמך מפרט מערכת המגדר.

צור את הפונקציות: `resolvePlaceholder`, `renderText`, `renderDocument`.

צור גם פונקציית עזר `extractPlaceholders(text: string): string[]` שמחזירה את כל ה-placeholders בטקסט.

**מוכן כאשר:**
- כל הפונקציות מיוצאות
- `renderText('{{מיופה.רשאי}}', ctx)` עם מיופה זכר מחזיר "רשאי"
- `renderText('{{מיופה.רשאי}}', ctx)` עם מיופה נקבה מחזיר "רשאית"
- `renderText('{{מיופה.רשאי}}', ctx)` עם שני מיופים מחזיר "רשאים"
- placeholder לא מזוהה מוחזר כמו שהוא: `{{שטויות.שטות}}`

---

### משימה 1.4 - בדיקות למנוע

**מה לעשות:**
צור את `src/lib/engine/renderer.test.ts` עם בדיקות vitest מקיפות:
- בדיקת כל סוגי הplaceholders (שדה, מילה, משתנה)
- בדיקת מגדר זכר, נקבה, רבים
- בדיקת שדות אישיים (שם, ת.ז., כתובת)
- בדיקת קוצים: placeholder ריק, שחקן לא קיים, מילה לא במילון
- בדיקת טקסט מורכב עם מספר placeholders

הוסף סקריפט `npm test` ל-`package.json`.

**מוכן כאשר:**
- `npm test` מריץ לפחות 20 בדיקות, כולן עוברות
- בדיקת coverage עוברת 80% על `renderer.ts`

---

### משימה 1.5 - שכבת מסד נתונים

**מה לעשות:**
צור את הקבצים הבאים תחת `src/lib/db/`, כל אחד עם פונקציות CRUD בסיסיות:

- `clients.ts`: `getClients`, `getClient`, `createClient`, `updateClient`, `deleteClient`
- `persons.ts`: `getPersons(clientId)`, `getPerson`, `createPerson`, `updatePerson`, `deletePerson`
- `documents.ts`: `getDocuments(clientId)`, `getDocument`, `createDocument`, `updateDocument`, `deleteDocument`, `saveDocumentVersion`
- `templates.ts`: `getTemplates(filters)`, `getTemplate`, `createUserTemplate`, `updateUserTemplate`, `deleteUserTemplate`

כל פונקציה מטפלת בשגיאות ומחזירה type נכון.

**מוכן כאשר:**
- כל הפונקציות עובדות מול Supabase האמיתי
- TypeScript types מתאימים
- אפשר ליצור client, להוסיף person ולשייך מסמך באמצעות הפונקציות

---

## שלב 2: ניהול לקוחות ואנשים

### משימה 2.1 - דף רשימת לקוחות

**מה לעשות:**
צור את `src/app/clients/page.tsx`:
- רשימת כל הלקוחות של המשתמש
- כפתור "לקוח חדש" שפותח מודל
- חיפוש לקוח לפי שם
- לחיצה על לקוח מעבירה ל-`/clients/[id]`

צור רכיב `<ClientCard>` שמציג שם הלקוח, מספר מסמכים, תאריך עדכון אחרון.

**מוכן כאשר:**
- ירון רואה רשימה ריקה כשמתחבר לראשונה
- אפשר ליצור לקוח חדש בשם "משפחת כהן"
- הלקוח מופיע ברשימה
- חיפוש "כהן" מסנן נכון

---

### משימה 2.2 - דף תיק לקוח

**מה לעשות:**
צור את `src/app/clients/[id]/page.tsx`:
- פרטי הלקוח (שם, הערות) עם אפשרות עריכה
- טאב "אנשים בתיק" עם רשימת persons + כפתור הוספה
- טאב "מסמכים" עם רשימת documents + כפתור "מסמך חדש"
- כפתור מחיקת לקוח (עם confirmation)

צור רכיב `<PersonForm>` ב-`src/components/person/PersonForm.tsx` שמטפל ביצירה ועריכה של אדם, כולל:
- שם פרטי + משפחה
- ת.ז. עם בדיקת תקינות (10 ספרות)
- מגדר (radio: זכר/נקבה)
- כתובת, עיר, טלפון, אימייל, תאריך לידה

**מוכן כאשר:**
- ירון יכול להוסיף את דוד כהן, שרה כהן, רחל כהן לתיק
- אפשר לערוך ולמחוק כל אדם
- ת.ז. עם 9 ספרות מציג שגיאה
- כל הנתונים נשמרים ב-Supabase

---

### משימה 2.3 - רכיב PersonPicker

**מה לעשות:**
צור את `src/components/person/PersonPicker.tsx` - רכיב שמאפשר לבחור אדם (או כמה אנשים) מתוך הפרסונים של הלקוח.

Props:
- `clientId: string`
- `value: string[]` - מערך IDs נבחרים
- `onChange: (ids: string[]) => void`
- `multiple?: boolean` - אם מותר לבחור יותר מאחד
- `label?: string`

הרכיב מציג רשימה עם checkboxes (אם multiple) או radio (אם לא).
מתחת לרשימה כפתור "הוסף אדם חדש" שפותח את ה-PersonForm במודל ומוסיף אוטומטית.

**מוכן כאשר:**
- אפשר לבחור אדם יחיד
- אפשר לבחור שני אנשים (במצב multiple)
- כפתור "הוסף חדש" יוצר אדם ובוחר אותו אוטומטית

---

## שלב 3: ספריית סעיפים

### משימה 3.1 - מבנה ספריית סעיפים ראשוני

**מה לעשות:**
צור תיקייה `templates/poa-property/` ובתוכה קבצי JSON עם 25 סעיפים ראשוניים לייפוי כוח רכושי. כל קובץ JSON עוקב אחר הסכימה של `SectionTemplate`.

הסעיפים (אני אכין אותם בנפרד, בינתיים placeholders עם הכותרות):
1. סעיף פתיחה
2. ביטול ייפויי כוח קודמים
3. הצהרת כשירות
4. מינוי מיופה כוח
5. מינוי מיופה כוח חלופי
6. ניהול חשבונות בנק - בסיסי
7. ניהול חשבונות בנק - מורחב
8. ניהול מקרקעין - השכרה בלבד
9. ניהול מקרקעין - כולל מכירה
10. ניהול מקרקעין - מוגבל בדירת מגורים
11. ניהול עסקים
12. ניהול תיק השקעות
13. תרומות וצדקה - מוגבל
14. תרומות וצדקה - חופשי
15. מתנות - מוגבל לקרובי משפחה
16. מתנות - חופשי
17. ניהול חובות והלוואות
18. ייצוג בהליכים משפטיים
19. ייצוג מול רשויות
20. כניסה לתוקף - מיידי
21. כניסה לתוקף - בעת אובדן כשירות
22. דיווח לאדם נוסף
23. סיום ייפוי הכוח
24. תיעוד פעולות
25. סעיף סיום וחתימה

צור סקריפט `scripts/seed-templates.ts` שטוען את כל הקבצים ומכניס ל-`section_templates` ב-Supabase.

**מוכן כאשר:**
- 25 קבצי JSON קיימים, כולם תקפים לפי הסכימה
- הסקריפט רץ ומכניס את כולם ל-DB
- שאילתת בחירה מ-`section_templates` מחזירה 25 רשומות

---

### משימה 3.2 - תוכן הסעיפים

**מה לעשות:**
מלא תוכן אמיתי בכל 25 הסעיפים, על בסיס המבנה הבא לכל סעיף:

```json
{
  "id": "poa-property-001",
  "category": "poa-property",
  "documentTypes": ["poa-property"],
  "title": "מינוי מיופה כוח",
  "description": "סעיף המינוי הבסיסי של מיופה הכוח",
  "variants": [
    {
      "id": "default",
      "label": "סטנדרטי",
      "content": "אני, {{ממנה.שם}} ת.ז. {{ממנה.תז}}, ..."
    },
    {
      "id": "restricted",
      "label": "מצומצם",
      "content": "..."
    },
    {
      "id": "expanded",
      "label": "מורחב",
      "content": "..."
    }
  ],
  "requiredActors": ["ממנה", "מיופה"],
  "legalBasis": "סעיף 32יד לחוק הכשרות המשפטית והאפוטרופסות",
  "isRequired": true,
  "conflictsWith": [],
  "tags": ["מינוי", "פתיחה"]
}
```

חשוב: השתמש בתחביר ה-placeholders לפי מסמך מפרט מערכת המגדר.

**מוכן כאשר:**
- כל הסעיפים מכילים תוכן משפטי מקצועי
- כל אחד מכיל לפחות וריאציה אחת (רצוי שלוש)
- אין שימוש בדפוסי `/ת`, `/ה` הישנים
- כל ה-placeholders הם בתחביר החדש `{{שחקן.מילה}}`

---

### משימה 3.3 - דף ספריית סעיפים

**מה לעשות:**
צור את `src/app/library/page.tsx`:
- רשימת כל הסעיפים מ-`section_templates`
- סינון לפי קטגוריה
- חיפוש בכותרת ובתוכן
- כל כרטיס מציג: כותרת, תיאור, וריאציות, תגיות
- לחיצה מציגה תצוגה מקדימה במודל

לא צריך עדיין לאפשר עריכה/יצירה - רק תצוגה.

**מוכן כאשר:**
- ירון רואה 25 סעיפים
- סינון לפי "ייפוי כוח רכושי" עובד
- חיפוש "השקעות" מציג רק את הסעיפים הרלוונטיים

---

## שלב 4: עורך מסמכים

### משימה 4.1 - יצירת מסמך חדש

**מה לעשות:**
צור flow ליצירת מסמך:
1. בתיק לקוח, כפתור "מסמך חדש"
2. מודל בחירת סוג: ייפוי כוח רכושי / אישי / רפואי / צוואה / וכו'. בשלב 1, רק רכושי פעיל.
3. אחרי בחירה, מסך הגדרת שחקנים:
   - בחירת ממנה (PersonPicker בודד)
   - בחירת מיופי כוח (PersonPicker מרובה)
   - בחירת מיופי כוח חלופיים (אופציונלי)
4. לחיצה על "צור מסמך" יוצרת record ומפנה ל-editor

**מוכן כאשר:**
- מתוך תיק לקוח אפשר ליצור מסמך ייפוי כוח חדש
- אפשר לבחור שחקנים מפרסונים קיימים
- ה-document נשמר ב-DB עם actors מאוכלסים

---

### משימה 4.2 - עורך מסמך - שלד

**מה לעשות:**
צור את `src/app/clients/[id]/documents/[docId]/page.tsx` עם פריסה של שלוש עמודות:

**עמודה שמאל - ספריית סעיפים:**
- רשימת סעיפים זמינים (מסוננים לפי סוג המסמך)
- כל סעיף הוא drag handle או כפתור "הוסף"
- חיפוש

**עמודה אמצעית - הסעיפים שנבחרו:**
- סדר את הסעיפים עם dnd-kit
- כל סעיף מציג: כותרת, וריאציה נבחרת, כפתור עריכה, כפתור מחיקה
- אפשר לשנות וריאציה מתוך dropdown

**עמודה ימנית - תצוגה מקדימה:**
- המסמך המלא אחרי rendering
- מתעדכן בזמן אמת כשמשנים סעיפים
- מציג בעיצוב דומה לפלט הסופי

**מוכן כאשר:**
- אפשר לגרור סעיף מספריית הסעיפים לרשימת המסמך
- אפשר לסדר מחדש סעיפים בעריכה
- תצוגה מקדימה מתעדכנת אוטומטית
- כל השמות והמגדרים נכונים בתצוגה

---

### משימה 4.3 - עורך סעיף בודד

**מה לעשות:**
לחיצה על "ערוך" בסעיף פותחת מודל עם:
- אפשרות לערוך את הטקסט (עם syntax highlighting ל-placeholders)
- אפשרות להחליף וריאציה
- תצוגה מקדימה של הסעיף עם הנתונים האמיתיים
- אזהרה אם יש placeholder לא תקין

צור רכיב `<PlaceholderEditor>` שמזהה placeholders ומציג אותם בצבע שונה.

**מוכן כאשר:**
- אפשר לערוך טקסט של סעיף
- placeholders מודגשים ויזואלית
- שינוי מתעדכן מיד בתצוגה המקדימה
- שמירה אוטומטית כל 2 שניות

---

### משימה 4.4 - משתנים גלובליים

**מה לעשות:**
הוסף לעורך פאנל "משתנים" עם:
- רשימת כל המשתנים שמופיעים בסעיפים (placeholders בלי נקודה)
- שדה הזנה לכל אחד
- שמירה ל-`document.variables`

לדוגמה, אם בסעיפים יש `{{תאריך_חתימה}}`, יופיע שדה "תאריך חתימה" שירון ימלא.

**מוכן כאשר:**
- כל המשתנים מהסעיפים מופיעים בפאנל
- מילוי שדה מעדכן את התצוגה המקדימה
- ערכים נשמרים ל-DB

---

## שלב 5: ייצוא

### משימה 5.1 - ייצוא לוורד

**מה לעשות:**
צור את `src/lib/export/word.ts` שמייצא מסמך לקובץ docx באמצעות הספרייה `docx`.

עיצוב נדרש:
- גופן David, גודל 12
- כותרות מודגשות
- שורת ריווח 1.5
- מספור אוטומטי של סעיפים ראשיים (1, 2, 3...)
- מספור היררכי לסעיפי משנה (1.1, 1.2...)
- שוליים סטנדרטיים
- כיוון RTL

צור כפתור "ייצא לוורד" בעורך המסמך שמוריד את הקובץ.

**מוכן כאשר:**
- לחיצה על "ייצא" מורידה קובץ docx
- הקובץ נפתח בוורד עם עיצוב נכון
- כל הסעיפים מופיעים עם השמות והמגדרים הנכונים
- אפשר להעתיק את התוכן ולהדביק במערכת האפוטרופוס הכללי

---

### משימה 5.2 - תצוגה לפני ייצוא

**מה לעשות:**
לפני ייצוא, הצג מודל עם:
- תצוגה מקדימה מלאה של המסמך
- צ'קליסט תיקוף בסיסי:
  - האם כל השחקנים הנדרשים מאוכלסים?
  - האם יש placeholders לא מטופלים?
  - האם יש סעיפים סותרים?
- כפתור "ייצא" או "חזור לעריכה"

**מוכן כאשר:**
- מודל נפתח לפני הייצוא
- צ'קליסט מציג אזהרות אדומות לבעיות
- אפשר עדיין לייצא גם עם אזהרות (אבל עם confirmation)

---

## שלב 6: ליטוש

### משימה 6.1 - שמירה אוטומטית

**מה לעשות:**
ודא שכל שינוי בעורך נשמר אוטומטית ל-Supabase תוך 2 שניות מהשינוי האחרון.

הצג אינדיקטור: "נשמר", "שומר...", "שגיאת שמירה".

**מוכן כאשר:**
- ניתן לערוך מסמך, לסגור דפדפן, ולמצוא את השינויים בחזרה
- אינדיקטור הסטטוס פועל נכון
- אין אובדן נתונים בעת ניווט בין דפים

---

### משימה 6.2 - תצוגת רשימת מסמכים

**מה לעשות:**
בדף תיק לקוח, שפר את רשימת המסמכים:
- כותרת המסמך + סוג
- תאריך עדכון אחרון
- סטטוס (טיוטה / לבדיקה / חתום)
- צבע לפי סוג המסמך
- חיפוש לפי שם
- מיון לפי תאריך, סוג, סטטוס

**מוכן כאשר:**
- אפשר למצוא מסמך לפי חיפוש שם
- מיון עובד נכון
- כל המידע מוצג ברור

---

### משימה 6.3 - שכפול מסמך

**מה לעשות:**
הוסף אפשרות "שכפל מסמך". יוצרת עותק של המסמך עם כל הסעיפים והשחקנים, בתור טיוטה חדשה.

שימושי כשירון רוצה לשנות מיופה כוח אבל לשמור את כל הסעיפים.

**מוכן כאשר:**
- כפתור "שכפל" עובד
- העותק החדש נפתח לעריכה
- המקור לא משתנה

---

## אחרי שלב 6 - מוכנים לפיילוט

בנקודה זו ירון יכול:
- ליצור תיק לקוח
- להזין את האנשים בתיק
- ליצור ייפוי כוח רכושי
- לבחור סעיפים ולערוך אותם
- לייצא לוורד ולהעלות לאפוטרופוס הכללי

הצעדים הבאים יהיו תוספות (היסטוריה, AI, סוגי מסמכים נוספים) ולא משפיעים על השימוש הבסיסי.

---

## הוראות עבודה לכל משימה

לכל משימה שתעבירי ל-Cursor:

**אחת.** העתיקי את גוף המשימה (מה לעשות + מוכן כאשר).
**שתיים.** הוסיפי בהתחלה: "קרא את הקבצים `01-architecture.md` ו-`02-gender-engine-spec.md`. הם המקור היחיד לאמת."
**שלוש.** הוסיפי בסוף: "כשתסיים, פרט מה עשית והעלה לבדיקה. אל תתחיל את המשימה הבאה לפני אישור."
**ארבע.** לפני אישור, בדקי בעצמך את ה-"מוכן כאשר".

ככה תמיד תדעי במדויק איפה את עומדת, ואם משהו נשבר, את יודעת באיזה שלב.

---

## אזהרות לעבודה עם Cursor

**אל תרשי לו לקצר תהליכים.** אם המשימה אומרת "כתוב 25 סעיפים", הוא לפעמים כותב 5 ו"דוגמאות". בקשי את כולם.

**ודאי שהוא מריץ בדיקות.** הוא נוטה לדלג. כל commit חייב להריץ `npm test` ו-`npm run build` ולעבור.

**אל תקפצי בין משימות.** הסדר חשוב. משימה 4 תלויה ב-3 ו-2 ו-1.

**כשמשהו לא עובד, אל תיתני לו "לסדר" עוד דברים.** קודם מאתרים את הבעיה הספציפית, מתקנים אותה, ורק אז ממשיכים.
